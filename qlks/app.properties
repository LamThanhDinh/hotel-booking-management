# Database configuration (do not commit real credentials)
# Switch to MySQL by setting db.enabled=true and providing url/user/password
# Example URL: jdbc:mysql://localhost:3306/hotel_management?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.enabled=true

# MySQL settings (only used when db.enabled=true)
db.url=jdbc:mysql://localhost:3307/hotel_management?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=
db.poolSize=5
